import { NombreTipoUnicoDirective } from './nombre-tipo-unico.directive';

describe('NombreTipoUnicoDirective', () => {
  it('should create an instance', () => {
    const directive = new NombreTipoUnicoDirective();
    expect(directive).toBeTruthy();
  });
});
