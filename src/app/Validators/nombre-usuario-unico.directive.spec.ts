import { NombreUsuarioUnicoDirective } from './nombre-usuario-unico.directive';

describe('NombreUsuarioUnicoDirective', () => {
  it('should create an instance', () => {
    const directive = new NombreUsuarioUnicoDirective();
    expect(directive).toBeTruthy();
  });
});
