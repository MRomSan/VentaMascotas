import { NoEspaciosBlancosDirective } from './no-espacios-blancos.directive';

describe('NoEspaciosBlancosDirective', () => {
  it('should create an instance', () => {
    const directive = new NoEspaciosBlancosDirective();
    expect(directive).toBeTruthy();
  });
});
